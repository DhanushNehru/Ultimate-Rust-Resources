# Parallel Domain Scanner

## Usage

```shell
# Scan a target
$ cargo run -- github.com
```

This code depicts the parallel processing capabilities provided by Rayon for domain scanning

This will display the subdomain along with the open ports